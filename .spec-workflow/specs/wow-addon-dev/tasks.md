# Tasks Document: wow-addon-dev

## Work Unit 1: Plugin Infrastructure

- [x] 1.1 Create marketplace manifest and plugin manifest
  - Files: `.claude-plugin/marketplace.json`, `plugins/wow-addon-dev/.claude-plugin/plugin.json`
  - Write marketplace.json with name, owner, description, and plugins array
  - Write plugin.json with name, version, description, homepage
  - Both files must be valid JSON (no trailing commas, no comments)
  - _Leverage: design.md C1 and C2 schemas (exact field names and values are specified there)_
  - _Requirements: R1, R2_
  - _Prompt: Implement the task for spec wow-addon-dev: Role: Plugin infrastructure developer specializing in JSON manifests and Claude Code plugin system | Task: Create two JSON manifest files that register the wow-addon-dev plugin with the Claude Code marketplace. File 1 is `.claude-plugin/marketplace.json` at the repo root — it contains the marketplace registration entry. File 2 is `plugins/wow-addon-dev/.claude-plugin/plugin.json` — it defines the installable plugin. Use the exact schemas specified in design.md components C1 and C2. Owner name is "Dennys Oliveira", GitHub URL is "https://github.com/DennysOliveira", repo URL is "https://github.com/DennysOliveira/wow-addon-dev", version is "1.0.0", category is "development". The `source` field in marketplace.json must be "./plugins/wow-addon-dev". The `skills` array must be ["./skills/wow-addon-dev"]. | Restrictions: Do NOT add any fields not present in the schemas in design.md C1/C2. Do NOT use trailing commas. Do NOT add JSON comments. Do NOT invent field names. | _Leverage: design.md sections C1 and C2 contain the exact schemas to use_ | _Requirements: R1, R2_ | Success: Both files parse without error via `JSON.parse`. The `source` field in marketplace.json resolves to the directory that will contain plugin.json. The `skills` entry resolves to a directory that will contain SKILL.md. Before starting, mark this task `[x]` in tasks.md. After completing, mark it `[x]`._

- [x] 1.2 Create SKILL.md
  - File: `plugins/wow-addon-dev/skills/wow-addon-dev/SKILL.md`
  - YAML frontmatter, routing table (8 rows), critical rules (6 rules), external resources (3 links)
  - Hard cap: 80 lines total (including blank lines)
  - _Leverage: design.md C3 (full structure specification with routing table content)_
  - _Requirements: R1, R2, R3, R4, R5, R6, R7, R8, R9_
  - _Prompt: Implement the task for spec wow-addon-dev: Role: Technical writer specializing in Claude Code skill authoring and progressive-disclosure knowledge design | Task: Create `plugins/wow-addon-dev/skills/wow-addon-dev/SKILL.md` — the always-loaded skill context file. This file has a hard cap of 80 lines (blank lines count). Structure must be: (1) YAML frontmatter with name "wow-addon-dev", version "1.0.0", description, and triggers list (triggers include: wow, world-of-warcraft, addon, lua, wowaddon, secret values, issecretvalue, midnight, interface 120001, widget, frame, ace3, libstub, toc); (2) H1 header identifying the skill; (3) "What" section — one-liner each: Lua 5.1 sandbox, Interface 120001, Secret Values; (4) routing table with exactly 8 rows as specified in design.md C3; (5) Critical Rules section with exactly 6 inline rules from design.md C3; (6) External Resources with 3 links: warcraft.wiki.gg, WoWAddonDevGuide (https://github.com/Amadeus-/WoWAddonDevGuide), wow-ui-source (https://github.com/Gethe/wow-ui-source). All reference doc paths in the routing table must use `${CLAUDE_SKILL_DIR}/references/filename.md` format. | Restrictions: Do NOT exceed 80 lines. Do NOT defer any of the 6 critical rules to reference docs — they must be inline. Do NOT reference COMBAT_LOG_EVENT_UNFILTERED anywhere. Do NOT add prose beyond what is needed to stay under 80 lines. | _Leverage: design.md section C3 contains the complete routing table and all 6 critical rules_ | _Requirements: R1 through R9_ | Success: `wc -l SKILL.md` returns 80 or fewer. All 8 routing table rows present. All 6 critical rules present inline. All 3 external resource links present. No mention of CLEU or COMBAT_LOG_EVENT_UNFILTERED. Before starting, mark this task `[x]` in tasks.md. After completing, mark it `[x]`._

- [x] 1.3 Create LICENSE, CHANGELOG.md, and issue template
  - Files: `LICENSE`, `CHANGELOG.md`, `.github/ISSUE_TEMPLATE/api-update.md`
  - LICENSE: MIT, copyright "Dennys Oliveira", year 2026
  - CHANGELOG.md: Keep a Changelog format, single entry [1.0.0] - 2026-04-06
  - api-update.md: GitHub issue template with YAML frontmatter and structured fields
  - _Leverage: design.md C6 (CHANGELOG and LICENSE specs), C6 issue template fields_
  - _Requirements: R10, R11, R12_
  - _Prompt: Implement the task for spec wow-addon-dev: Role: Open source project maintainer with expertise in GitHub repository standards | Task: Create three repository support files. (1) `LICENSE`: Standard MIT license text, copyright year 2026, copyright holder "Dennys Oliveira". (2) `CHANGELOG.md`: Follow Keep a Changelog format (https://keepachangelog.com). Single entry `[1.0.0] - 2026-04-06` under an Added section. List all files introduced, grouped by type: Manifests (.claude-plugin/marketplace.json, plugins/wow-addon-dev/.claude-plugin/plugin.json), Skill (SKILL.md), References (7 reference docs), Templates (basic-addon and secret-aware-addon files), Root (README.md, LICENSE, CHANGELOG.md). (3) `.github/ISSUE_TEMPLATE/api-update.md`: GitHub issue template with YAML frontmatter fields `name: "API Update Report"`, `about: "Report a WoW API change that affects this skill"`, `labels: ["api-update"]`. Body fields: API name, old behaviour, new behaviour, source/reference URL, affected addon code snippet (fenced code block), version confirmed (interface number where change was observed). | Restrictions: Do NOT add co-authored-by lines. Do NOT use placeholder text. MIT license body must be the standard OSI-approved text verbatim. | _Leverage: design.md section C6_ | _Requirements: R10, R11, R12_ | Success: LICENSE contains valid MIT text with correct year and holder. CHANGELOG.md lists all ~25 repository files. api-update.md renders as a GitHub issue template with all required fields. Before starting, mark this task `[x]` in tasks.md. After completing, mark it `[x]`._

---

## Work Unit 2: Reference Docs Group A (Midnight-Critical)

- [x] 2.1 Create secret-values.md reference
  - File: `plugins/wow-addon-dev/skills/wow-addon-dev/references/secret-values.md`
  - Covers: issecretvalue(), HasSecretValues(), HasSecretAspect(), Curve, ColorCurve, Duration, UnitHealPredictionCalculator, C_RestrictedActions, C_Secrets, working health bar example
  - _Leverage: wow-addon-skill-research.md (primary source for all API signatures and behavior)_
  - _Requirements: R3_
  - _Prompt: Implement the task for spec wow-addon-dev: Role: WoW addon developer specializing in Midnight 12.0+ Secret Values system and secure UI patterns | Task: Create `plugins/wow-addon-dev/skills/wow-addon-dev/references/secret-values.md` — a self-contained reference covering the entire Secret Values system introduced in Midnight 12.0. Required sections and content: (1) H1 title with "(April 2026)" date marker; (2) `issecretvalue(value)` — exact signature, return type (boolean), when to call it, why arithmetic on secret values is forbidden; (3) `HasSecretValues()` and `HasSecretAspect(aspect)` on FrameScriptObject — signatures and when each fires; (4) `Curve` type — `C_CurveUtil.CreateCurve()` signature, how key-value pairs work, `Curve:Evaluate(x)`, `Curve:EvaluateNormalized(x)` with example; (5) `ColorCurve` type — `C_CurveUtil.CreateColorCurve()`, `ColorCurve:Evaluate(x)` returning r, g, b, a with a concrete green-to-red gradient example; (6) `Duration` type — `C_DurationUtil.CreateDuration()` and usage for time-based secret data; (7) `UnitHealPredictionCalculator` — `CreateUnitHealPredictionCalculator()`, constructor params, usage pattern; (8) `C_RestrictedActions` namespace — list all functions with signatures; (9) `C_Secrets` namespace — list all functions with signatures; (10) Complete working health bar example using ColorCurve applied to StatusBar:SetValue(), driven by issecretvalue() check. All Lua code must be valid WoW Lua 5.1 syntax. No goto, no bitwise ~, no // integer division. Interface version references must use 120001. Add a table of contents. | Restrictions: Do NOT reference COMBAT_LOG_EVENT_UNFILTERED. Do NOT include TODO or placeholder text. Do NOT cross-reference other reference docs as required reading. Do NOT perform arithmetic on secret values in any example. This doc must be independently readable. | _Leverage: wow-addon-skill-research.md at F:/dev/wow-addon-dev/wow-addon-skill-research.md is the PRIMARY content source — read it first_ | _Requirements: R3_ | Success: All 9 content sections present. Working Lua health bar example is syntactically valid. issecretvalue() is called before any StatusBar operation. No arithmetic on raw health values. No CLEU reference. Before starting, mark this task `[x]` in tasks.md. After completing, mark it `[x]`._

- [x] 2.2 Create api-migration-12.md reference
  - File: `plugins/wow-addon-dev/skills/wow-addon-dev/references/api-migration-12.md`
  - Covers: CLEU removal and replacements, deprecated-to-replacement mapping table, new 12.0 APIs, spell whitelisting, instance restrictions
  - _Leverage: wow-addon-skill-research.md (primary source), design.md C4b_
  - _Requirements: R4_
  - _Prompt: Implement the task for spec wow-addon-dev: Role: WoW addon developer specializing in API migration, deprecation tracking, and Midnight 12.0 compatibility | Task: Create `plugins/wow-addon-dev/skills/wow-addon-dev/references/api-migration-12.md` — a self-contained reference for migrating addons from pre-12.0 to Midnight. Required sections: (1) H1 title with "(April 2026)" date marker; (2) Table of contents; (3) CLEU removal — explain what COMBAT_LOG_EVENT_UNFILTERED was, why it was removed in 12.0, and what replaces it (UNIT_SPELLCAST and unit-based events) — include a migration example showing old pattern replaced by new pattern; (4) Deprecated-to-replacement mapping table organized by deprecated Lua file — cover all six files: Deprecated_CombatLog.lua, Deprecated_BattleNet.lua, Deprecated_ChatInfo.lua, Deprecated_SpellBook.lua, Deprecated_InstanceEncounter.lua, Deprecated_SpellScript.lua — each row shows deprecated function → replacement; (5) New 12.0 APIs not present in earlier versions — list with brief descriptions; (6) Spell whitelisting system — definition, how addons request spell inclusion, behavior for non-whitelisted spells; (7) Instance restrictions — which APIs are restricted inside instances, enforcement mechanism. All Lua examples must be valid WoW Lua 5.1. | Restrictions: The section about CLEU must explain the removal but must NOT show COMBAT_LOG_EVENT_UNFILTERED as a usable API — frame it as "was removed" only. No TODO or placeholder text. No cross-reference dependencies on other docs. | _Leverage: wow-addon-skill-research.md at F:/dev/wow-addon-dev/wow-addon-skill-research.md is the PRIMARY content source_ | _Requirements: R4_ | Success: All 7 sections present. Deprecated mapping table covers all 6 deprecated Lua files. Spell whitelisting section explains the mechanism clearly. Instance restrictions section lists affected APIs. Before starting, mark this task `[x]` in tasks.md. After completing, mark it `[x]`._

- [x] 2.3 Create toc-structure.md reference
  - File: `plugins/wow-addon-dev/skills/wow-addon-dev/references/toc-structure.md`
  - Covers: Interface 120001, multi-edition format, all new 12.0 TOC fields, edition-specific file naming, complete annotated example
  - _Leverage: wow-addon-skill-research.md, design.md C4c_
  - _Requirements: R5_
  - _Prompt: Implement the task for spec wow-addon-dev: Role: WoW addon developer specializing in addon packaging, TOC format, and multi-edition compatibility | Task: Create `plugins/wow-addon-dev/skills/wow-addon-dev/references/toc-structure.md` — a self-contained reference for WoW 12.0 TOC file format. Required sections: (1) H1 title with "(April 2026)" date marker; (2) Table of contents; (3) `## Interface: 120001` — explain this is the required value for Midnight, what the number encodes (major 12, minor 00, patch 01); (4) Multi-edition comma-delimited format: `## Interface: 120001, 11503` — when to use it, what each version targets; (5) All new TOC fields in 12.0: `## Category:`, `## Group:`, `## AllowAddOnTableAccess:`, `## LoadSavedVariablesFirst:`, `## AllowLoadGameType:`, `## LoadFirst:` — each with purpose and valid values; (6) Edition-specific file naming: `_Classic.toc`, `_Vanilla.toc`, `_Cata.toc` with directory layout diagram; (7) Complete annotated example TOC file using Interface 120001 — every field commented, at least 10 fields shown. | Restrictions: Every `## Interface:` value in this document must be 120001 (or a multi-edition list that includes 120001). No placeholder addon names — use "MyAddon" consistently. No TODO or stub content. | _Leverage: wow-addon-skill-research.md at F:/dev/wow-addon-dev/wow-addon-skill-research.md is the PRIMARY content source_ | _Requirements: R5_ | Success: All 7 sections present. Complete annotated TOC example present with 120001 as interface value. All 6 new 12.0 TOC fields documented with valid values. Before starting, mark this task `[x]` in tasks.md. After completing, mark it `[x]`._

---

## Work Unit 3: Reference Docs Group B (General Addon Dev)

- [x] 3.1 Create addon-scaffolding.md reference
  - File: `plugins/wow-addon-dev/skills/wow-addon-dev/references/addon-scaffolding.md`
  - Covers: minimal file structure, namespace pattern, SavedVariables, slash commands, event handling, Hello World walkthrough
  - _Leverage: wow-addon-skill-research.md, design.md C4d_
  - _Requirements: R6_
  - _Prompt: Implement the task for spec wow-addon-dev: Role: WoW addon developer specializing in addon architecture and Lua best practices | Task: Create `plugins/wow-addon-dev/skills/wow-addon-dev/references/addon-scaffolding.md` — a self-contained reference for creating a valid WoW 12.0 addon from scratch. Required sections: (1) H1 title with "(April 2026)" date marker; (2) Minimal required file structure for a valid 12.0 addon (TOC + at least one Lua file); (3) Namespace pattern: `local AddonName, ns = ...` — why it avoids global pollution, how to share the namespace across files; (4) SavedVariables — how to declare in TOC (`## SavedVariables: MyAddonDB`), how to access via ADDON_LOADED event, version-gated defaults initialization pattern; (5) Slash command registration: `SlashCmdList["MYADDON"]` and `SLASH_MYADDON1 = "/myaddon"` pattern; (6) Event handling: `frame:RegisterEvent()` and `frame:SetScript("OnEvent", ...)` dispatcher pattern with if/elseif chain; (7) Complete Hello World walkthrough — a full TOC + Lua pair that uses Interface 120001, registers for ADDON_LOADED and PLAYER_LOGIN, prints a message to chat. The walkthrough Lua must be syntactically valid and complete — no stubs, no TODO. | Restrictions: No TODO, no placeholder text, no ellipsis stubs. All Lua must be valid WoW Lua 5.1. Interface value must be 120001. No CLEU reference. This doc must be independently readable without other reference docs. | _Leverage: wow-addon-skill-research.md at F:/dev/wow-addon-dev/wow-addon-skill-research.md is the PRIMARY content source_ | _Requirements: R6_ | Success: All 7 sections present. Complete Hello World TOC+Lua pair is present and syntactically valid. Namespace pattern shown correctly. SavedVariables initialization pattern present. Before starting, mark this task `[x]` in tasks.md. After completing, mark it `[x]`._

- [x] 3.2 Create lua-api-quick-ref.md reference
  - File: `plugins/wow-addon-dev/skills/wow-addon-dev/references/lua-api-quick-ref.md`
  - Covers: exactly 50 API entries in 5 categories, each with signature, description, and secret/safe marker
  - _Leverage: wow-addon-skill-research.md, design.md C4e_
  - _Requirements: R7_
  - _Prompt: Implement the task for spec wow-addon-dev: Role: WoW API documentation author with deep knowledge of the Lua 5.1 addon API surface | Task: Create `plugins/wow-addon-dev/skills/wow-addon-dev/references/lua-api-quick-ref.md` — a quick-reference table of exactly 50 WoW API functions for Midnight 12.0. Structure: (1) H1 title with "(April 2026)" date marker; (2) Legend section explaining markers: 🔒 = secret-value-affected (do not perform arithmetic on results), ✅ = safe to use directly; (3) Table of contents linking to 5 categories; (4) Five category tables, each with columns: Function Signature | Description | Marker. Categories and counts: Unit (12 entries), Spell (10 entries), Combat (8 entries), UI/Frame (12 entries), Utility (8 entries). Total must be exactly 50 entries. Draw entries from the most commonly used APIs in 12.0 addon development. APIs that return health, power, absorb, or combat values that may be secret must be marked 🔒. Safe utility and UI APIs must be marked ✅. | Restrictions: Total entry count must be exactly 50 — count carefully. No COMBAT_LOG_EVENT_UNFILTERED in any entry. No deprecated APIs unless explicitly noted as deprecated. No placeholder entries. All function signatures must use real WoW 12.0 API names. | _Leverage: wow-addon-skill-research.md at F:/dev/wow-addon-dev/wow-addon-skill-research.md is the PRIMARY content source for API names and signatures_ | _Requirements: R7_ | Success: Exactly 50 entries — count them. All 5 categories present with correct entry counts (12+10+8+12+8=50). Every entry has a 🔒 or ✅ marker. No CLEU reference. Before starting, mark this task `[x]` in tasks.md. After completing, mark it `[x]`._

- [x] 3.3 Create widget-framework.md reference
  - File: `plugins/wow-addon-dev/skills/wow-addon-dev/references/widget-framework.md`
  - Covers: CreateFrame() types, event/script handlers, XML syntax, Mixin, anchoring, secure vs insecure frames, working unit frame example
  - _Leverage: wow-addon-skill-research.md, design.md C4f_
  - _Requirements: R8_
  - _Prompt: Implement the task for spec wow-addon-dev: Role: WoW UI developer specializing in frame scripting, XML templates, and the Blizzard widget framework | Task: Create `plugins/wow-addon-dev/skills/wow-addon-dev/references/widget-framework.md` — a self-contained reference for the WoW 12.0 widget system. Required sections: (1) H1 title with "(April 2026)" date marker; (2) Table of contents; (3) Primary frame types via `CreateFrame()` — cover Frame, Button, StatusBar, Slider, EditBox, ScrollFrame, MessageFrame, Cooldown, Model, PlayerModel — for each: brief description of distinguishing capabilities; (4) Event system and script handlers — OnEvent, OnLoad, OnUpdate, OnShow, OnHide, OnEnter, OnLeave — each with parameter list and typical usage; (5) XML template syntax — `<Frames>`, `<Frame>`, `<Scripts>` blocks, template inheritance with `inherits` attribute, complete minimal XML example; (6) Mixin usage — `Mixin(object, Mixin1, Mixin2)` and `CreateAndInitFromMixin()` with examples; (7) Frame anchoring — `SetPoint(point, relativeTo, relativePoint, xOffset, yOffset)`, `ClearAllPoints()`, strata values (BACKGROUND through TOOLTIP); (8) Secure vs insecure frames in 12.0 — what causes taint, SecureHandlerStateTemplate, restricted environment limitations; (9) Complete working unit frame example — health bar + name text, event-driven updates, OnUpdate pattern. All Lua and XML must be syntactically valid. | Restrictions: No CLEU reference. No TODO or stubs. The working unit frame example must be complete and functional. Must be independently readable. | _Leverage: wow-addon-skill-research.md at F:/dev/wow-addon-dev/wow-addon-skill-research.md is the PRIMARY content source_ | _Requirements: R8_ | Success: All 9 sections present. Working unit frame example is complete and syntactically valid. All 10 CreateFrame types documented. All 7 script handlers documented with parameter lists. Before starting, mark this task `[x]` in tasks.md. After completing, mark it `[x]`._

- [x] 3.4 Create common-patterns.md reference
  - File: `plugins/wow-addon-dev/skills/wow-addon-dev/references/common-patterns.md`
  - Covers: Ace3, LibStub, LibDataBroker, LibSharedMedia, SavedVariables migration, addon comms, 3+ performance patterns
  - _Leverage: wow-addon-skill-research.md, design.md C4g_
  - _Requirements: R9_
  - _Prompt: Implement the task for spec wow-addon-dev: Role: WoW addon developer specializing in library ecosystems, performance optimization, and addon communication patterns | Task: Create `plugins/wow-addon-dev/skills/wow-addon-dev/references/common-patterns.md` — a self-contained reference for common WoW addon patterns. Required sections: (1) H1 title with "(April 2026)" date marker; (2) Table of contents; (3) Ace3 — `AceAddon-3.0` initialization (`NewAddon`, `OnInitialize`, `OnEnable` lifecycle), `AceEvent-3.0` (`RegisterEvent`), `AceDB-3.0` (profile setup, `RegisterDefaults`) — complete minimal Ace3 addon example; (4) LibStub — embedding pattern, `LibStub("LibName-1.0", [silent])` call pattern; (5) LibDataBroker — data source creation, minimap button via LibDBIcon — with code example; (6) LibSharedMedia — registering and retrieving textures/sounds/fonts; (7) SavedVariables migration pattern — version-gated defaults check in OnInitialize, how to migrate old data; (8) Addon communications — `C_ChatInfo.SendAddonMessage(prefix, message, channel)`, prefix registration via `C_ChatInfo.RegisterAddonMessagePrefix()`, instance restriction note (blocked inside instances in 12.0); (9) Performance patterns — minimum 3: (a) throttling OnUpdate with elapsed accumulator, (b) frame pooling with `CreateFramePool()`, (c) lazy loading modules via ADDON_LOADED. All Lua must be valid WoW Lua 5.1. | Restrictions: No CLEU reference. No TODO or stubs. The instance restriction for addon comms must be explicitly noted. Must be independently readable. | _Leverage: wow-addon-skill-research.md at F:/dev/wow-addon-dev/wow-addon-skill-research.md is the PRIMARY content source_ | _Requirements: R9_ | Success: All 9 sections present. Ace3 example is a complete working addon snippet. All 3 performance patterns documented with code. Instance restriction for SendAddonMessage noted. Before starting, mark this task `[x]` in tasks.md. After completing, mark it `[x]`._

---

## Work Unit 4: Templates

- [x] 4.1 Create basic-addon template
  - Files: `plugins/wow-addon-dev/templates/basic-addon/MyAddon.toc`, `plugins/wow-addon-dev/templates/basic-addon/MyAddon.lua`, `plugins/wow-addon-dev/templates/basic-addon/README.md`
  - Real working code — no placeholders, no TODO, no stubs
  - _Leverage: design.md C5a, wow-addon-skill-research.md_
  - _Requirements: R6, R10_
  - _Prompt: Implement the task for spec wow-addon-dev: Role: WoW addon developer creating a production-quality starter template | Task: Create the basic-addon template — three files that form a complete, copy-ready WoW addon. (1) `plugins/wow-addon-dev/templates/basic-addon/MyAddon.toc`: Must include `## Interface: 120001`, `## Title: MyAddon`, `## Notes: A minimal WoW addon template`, `## Author: YourName`, `## Version: 1.0.0`, `## SavedVariables: MyAddonDB`, and `MyAddon.lua` as the file entry. (2) `plugins/wow-addon-dev/templates/basic-addon/MyAddon.lua`: Must use `local AddonName, ns = ...` namespace pattern. Must register for ADDON_LOADED event and initialize SavedVariables with version-gated defaults (if MyAddonDB is nil or MyAddonDB.version < 1, set defaults). Must register slash command `/myaddon` that prints a status message to chat. Must register for PLAYER_LOGIN and print a greeting. Code must be syntactically valid and execute correctly as-is in WoW 12.0. (3) `plugins/wow-addon-dev/templates/basic-addon/README.md`: Must explain what this addon does, the installation path (`Interface/Addons/MyAddon/` inside `_retail_`), and a customization guide with steps to rename the addon, change the slash command, and change the output message. | Restrictions: No TODO comments. No `[YOUR CODE HERE]` placeholders. No ellipsis stubs. No CLEU reference. Lua must be valid WoW Lua 5.1 — no goto, no bitwise ~, no Lua 5.2+ syntax. Interface version must be 120001. The code must work without modification when dropped into WoW. | _Leverage: design.md C5a for exact requirements, wow-addon-skill-research.md for Lua patterns_ | _Requirements: R6, R10_ | Success: MyAddon.toc has Interface 120001. MyAddon.lua is syntactically valid Lua 5.1, contains issecretvalue check is NOT required here (this is the basic template, not secret-aware). SavedVariables init present. Slash command present. PLAYER_LOGIN handler present. README has install path and rename instructions. Before starting, mark this task `[x]` in tasks.md. After completing, mark it `[x]`._

- [x] 4.2 Create secret-aware-addon template
  - Files: `plugins/wow-addon-dev/templates/secret-aware-addon/MyAddon.toc`, `Core.lua`, `SecretHandlers.lua`, `UI.xml`, `README.md`
  - Must use issecretvalue(), C_CurveUtil.CreateColorCurve(), StatusBar:SetValue()
  - _Leverage: design.md C5b, wow-addon-skill-research.md (PRIMARY source for Secret Values API signatures)_
  - _Requirements: R3, R10_
  - _Prompt: Implement the task for spec wow-addon-dev: Role: WoW addon developer specializing in Midnight 12.0 Secret Values integration and secure UI patterns | Task: Create the secret-aware-addon template — five files demonstrating the correct Secret Values pattern. (1) `plugins/wow-addon-dev/templates/secret-aware-addon/MyAddon.toc`: `## Interface: 120001`, standard metadata, `## SavedVariables: MyAddonDB`, file list: Core.lua, SecretHandlers.lua, UI.xml. (2) `plugins/wow-addon-dev/templates/secret-aware-addon/Core.lua`: Addon namespace declaration via `local AddonName, ns = ...`, event framework setup (main event frame, RegisterEvent, SetScript OnEvent dispatcher), initialization sequence that calls into SecretHandlers on ADDON_LOADED. (3) `plugins/wow-addon-dev/templates/secret-aware-addon/SecretHandlers.lua`: Must contain `issecretvalue()` check before operating on health values. Must create a ColorCurve via `C_CurveUtil.CreateColorCurve()` with a green-to-red gradient (at minimum 3 key points: 0.0→green, 0.5→yellow, 1.0→red). Must call `ColorCurve:Evaluate(healthPct)` to get r,g,b. Must call `StatusBar:SetValue()` with the raw secret health value. Must call `StatusBar:SetStatusBarColor()` with the evaluated color values. (4) `plugins/wow-addon-dev/templates/secret-aware-addon/UI.xml`: Valid Blizzard XML — `<Ui>` root, `<Frames>` block, `<Frame>` containing a `<StatusBar>` named "MyAddonHealthBar", `<Scripts>` block with OnLoad handler, dimensions and anchor defined. (5) `plugins/wow-addon-dev/templates/secret-aware-addon/README.md`: Explains the Secret Values pattern used, why issecretvalue() must be checked, why arithmetic must not be performed on the raw health value, and how ColorCurve bridges the gap. | Restrictions: No TODO or placeholder text. No arithmetic on raw health/secret values (no division, multiplication, or subtraction on the secret value itself). No CLEU reference. All Lua must be valid WoW Lua 5.1. Interface must be 120001. The OnLoad in UI.xml must reference the correct global function name defined in Core.lua or SecretHandlers.lua. | _Leverage: wow-addon-skill-research.md at F:/dev/wow-addon-dev/wow-addon-skill-research.md — read this FIRST for exact Secret Values API signatures. design.md C5b for structure._ | _Requirements: R3, R10_ | Success: SecretHandlers.lua contains issecretvalue(), C_CurveUtil.CreateColorCurve(), ColorCurve:Evaluate(), StatusBar:SetValue(), StatusBar:SetStatusBarColor(). No arithmetic on secret values. UI.xml is valid XML with StatusBar named MyAddonHealthBar. README explains the why. Before starting, mark this task `[x]` in tasks.md. After completing, mark it `[x]`._

---

## Work Unit 5: README.md

- [x] 5.1 Create repository README.md
  - File: `README.md` (repo root)
  - Four badges, Quick Install 2-command block, What You Get, 5 Usage Examples, Alternative install, What's Inside table, Contributing, Midnight Context, Related Resources, License footer
  - _Leverage: design.md C6 (README), WU-1 manifest content for install commands_
  - _Requirements: R1, R10, R11, R12_
  - _Prompt: Implement the task for spec wow-addon-dev: Role: Developer relations engineer specializing in open source project documentation and GitHub README design | Task: Create `README.md` at the repository root — the GitHub-facing project page. Required sections in order: (1) Four inline badge images (no external badge services needed — use shields.io style): MIT License, Claude Code Plugin, WoW Midnight 12.0+, API Updated April 2026; (2) "Quick Install" section — two commands in a fenced code block: first command adds the marketplace (`/plugin marketplace add DennysOliveira/wow-addon-dev`), second activates it; (3) "What You Get" — bullet list with short descriptions of the skill, 7 reference docs, and 2 templates; (4) "Usage Examples" — 5 fenced code blocks showing prompts a user can paste into Claude, e.g., "How do I detect if a health value is secret?", "Show me the API migration from CLEU to Midnight events", "Create a new addon skeleton", "How do I create a health bar with ColorCurve?", "What TOC fields are new in 12.0?"; (5) "Alternative Install" — git clone command + instruction to symlink or copy the plugin directory into CLAUDE.md; (6) "What's Inside" — markdown table with 7 rows, one per reference doc, columns: Document | What It Covers; (7) "Contributing" — focus on API update reports, link to `.github/ISSUE_TEMPLATE/api-update.md`; (8) "Midnight Context" — 2-3 sentences explaining what the Midnight expansion change means for addon devs (Secret Values, CLEU removal, interface 120001); (9) "Related Resources" — links to warcraft.wiki.gg Secret Values page, WoWAddonDevGuide, wow-ui-source (Gethe), Patch 12.0.0 API Changes; (10) License footer line. | Restrictions: Do not reference COMBAT_LOG_EVENT_UNFILTERED in the examples section as a usable API. No placeholder text. No TODO. Badge URLs must use real shields.io-style format but can use static labels (no dynamic badges requiring external queries). | _Leverage: design.md section C6 README specification_ | _Requirements: R1, R10, R11, R12_ | Success: All 10 sections present. Quick Install block contains 2 commands. Exactly 5 usage example prompts. What's Inside table has exactly 7 rows. No CLEU shown as usable. Before starting, mark this task `[x]` in tasks.md. After completing, mark it `[x]`._

---

## Verification Checklist (run after all tasks complete)

- [ ] V.1 JSON validation: `marketplace.json` and `plugin.json` parse without errors
- [ ] V.2 SKILL.md line count: `wc -l` returns 80 or fewer
- [ ] V.3 Interface version: every `## Interface:` value in any TOC file is `120001`
- [ ] V.4 No CLEU: full-text search for `COMBAT_LOG_EVENT_UNFILTERED` returns zero results
- [ ] V.5 No placeholders: no file contains `TODO`, `[YOUR`, `lorem`, or a standalone `...` stub line
- [ ] V.6 API count: `lua-api-quick-ref.md` contains exactly 50 entries
- [ ] V.7 Secret Values template: `SecretHandlers.lua` contains `issecretvalue(`, `CreateColorCurve(`, and `SetValue(`
- [ ] V.8 Path resolution: `source` field in `marketplace.json` resolves to directory containing `plugin.json`; `skills` entry resolves to directory containing `SKILL.md`
- [ ] V.9 All 25 files exist at expected paths per design.md directory tree
